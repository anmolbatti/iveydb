# Enable rewrite engine
RewriteEngine On
RewriteBase /

# Redirect all requests to the index.html file in the build folder
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
